# 測試結果

**專案：** WuWaGroup
**對應階段：** 第 1 階段（建立資料庫 Model、Migration、Enum、Constraint；建立專案骨架與開發環境）
**記錄日期：** 2026-07-21（首次撰寫：僅靜態複查）／2026-07-21（更新：實際執行完成）
**資料庫：** Supabase PostgreSQL（專案 `kjziqgiqiwknxqejancr`，region `ap-south-1`）

---

## 0. 更新說明

本文件第一版僅完成「靜態複查」，因當時環境沒有 Python／Node.js／PostgreSQL 可實際執行。之後已透過 winget 在本機安裝 Python 3.12.10、Node.js v24.18.0，並取得使用者的 Supabase 連線資訊，**實際執行了 pytest 與 `alembic upgrade head`**。以下第 1、2 節為原始靜態複查紀錄（保留作歷史紀錄），第 3 節為新增的**實測結果**，以第 3 節為準。

---

## 1. 靜態複查（開發時發現並修正的問題）

逐檔手動審閱 Stage 1 程式碼時發現並修正：

| # | 問題 | 影響檔案 | 修正方式 |
|---|---|---|---|
| 1 | SQLAlchemy Enum 欄位的 `server_default` 直接傳入未加引號的字串（如 `server_default="member"`），會產生無效 SQL `DEFAULT member` | `app/models/user.py`、`group_leader.py`、`activity.py`、`group_buy.py`、`order.py`（共 6 處） | 改用 `server_default=text("'member'")`，確保產生 `DEFAULT 'member'` |

其餘靜態複查結論（Enum 型別重用、Migration 採原生 SQL、外鍵建立順序人工核對）詳見專案歷史紀錄，實測結果已在第 3 節完整驗證，不再重複列出。

---

## 2. 已撰寫的測試清單

| 測試檔案 | 測試內容 | 是否需要真實資料庫連線 |
|---|---|---|
| `backend/tests/test_models_metadata.py` | 驗證 `Base.metadata` 中恰好註冊 18 張資料表；驗證 `app_user` 聯絡方式欄位可為 NULL 且有對應 CHECK Constraint；驗證 `product.official_price`／`official_currency` 擴充欄位存在且可為 NULL；驗證 `activity.category` 為自由文字（String）且可為 NULL；驗證 `group_order` 具備所有快照欄位；驗證 `order_item`、`product_favorite` 的 UniqueConstraint 名稱存在 | 否（純 Python metadata 檢查） |
| `backend/tests/test_health.py` | 使用 FastAPI `TestClient` 呼叫 `GET /api/v1/health`，驗證回傳 `200` 且內容為 `{"data": {"status": "ok"}}` | 否 |

尚未撰寫（待 Stage 2 起陸續補上）：API 端點整合測試、Service Layer 業務規則測試、Transaction／Row Lock 併發測試。

---

## 3. 實際執行結果（2026-07-21）

### 3.1 開發環境安裝

透過 winget 安裝：
- `Python.Python.3.12` → 實際版本 **Python 3.12.10**
- `OpenJS.NodeJS.LTS` → 實際版本 **Node.js v24.18.0**、**npm 11.16.0**

於 `backend/` 建立虛擬環境並安裝 `requirements.txt`，全部套件安裝成功，無版本衝突。

### 3.2 pytest 實際執行結果

```
platform win32 -- Python 3.12.10, pytest-8.3.3, pluggy-1.6.0
collected 7 items

tests/test_health.py::test_health_check_returns_ok PASSED                [ 14%]
tests/test_models_metadata.py::test_all_18_tables_registered PASSED      [ 28%]
tests/test_models_metadata.py::test_app_user_contact_columns_nullable_and_required_by_check_constraint PASSED [ 42%]
tests/test_models_metadata.py::test_product_official_price_and_currency_extension_columns_exist PASSED [ 57%]
tests/test_models_metadata.py::test_activity_category_extension_column_is_free_text_and_nullable PASSED [ 71%]
tests/test_models_metadata.py::test_group_order_has_required_snapshot_columns PASSED [ 85%]
tests/test_models_metadata.py::test_unique_constraints_exist_for_pending_style_business_rules PASSED [100%]

7 passed in 0.69s
```

**結果：7/7 通過。**（第一次執行時出現 `pytest-asyncio` 的 `PytestDeprecationWarning`，已在 `pytest.ini` 加入 `asyncio_default_fixture_loop_scope = function` 消除，重跑後無任何警告。）

### 3.3 Alembic Migration 實際執行結果

**過程中排除的連線問題（記錄供未來參考）：**

1. 第一次使用 Supabase **Direct connection**（`db.kjziqgiqiwknxqejancr.supabase.co:5432`）→ 失敗，`psycopg.OperationalError: [Errno 11001] getaddrinfo failed`。原因：Supabase Direct connection 預設走 IPv6，本機網路無法解析。
2. 改用 **Session pooler**，但主機名稱猜測為 `aws-0-ap-south-1.pooler.supabase.com` → 失敗，`FATAL: tenant/user postgres.kjziqgiqiwknxqejancr not found`。原因：pooler 節點前綴猜錯（應為 `aws-1`，不是 `aws-0`）。
3. 改用使用者從 Supabase Dashboard「Connect」視窗複製的正確字串 `aws-1-ap-south-1.pooler.supabase.com:5432` → **連線成功**。

**最終可用的 `DATABASE_URL`（已寫入 `backend/.env`，未提交至版本控制）：**
```
postgresql+psycopg://postgres.kjziqgiqiwknxqejancr:****@aws-1-ap-south-1.pooler.supabase.com:5432/postgres
```

**`alembic upgrade head` 執行輸出：**
```
INFO  [alembic.runtime.migration] Context impl PostgresqlImpl.
INFO  [alembic.runtime.migration] Will assume transactional DDL.
INFO  [alembic.runtime.migration] Running upgrade  -> 0001_initial_schema, initial schema: 18 core tables + enums + constraints + indexes
```
`alembic current` 確認版本為 `0001_initial_schema (head)`。**執行成功，無錯誤。**

### 3.4 資料庫實際結構驗證

以一次性腳本直接查詢 Supabase `information_schema` / `pg_catalog`（執行後已刪除，不屬於正式程式碼）：

- **資料表：19 張**（18 張規格資料表 + Alembic 自己的 `alembic_version` 追蹤表），與 `04_Database_Design_v2.1` 完全一致。
- **自訂 Enum：12 個** —— `activity_status`、`announcement_audience_scope`、`announcement_type`、`cancellation_status`、`contact_platform`、`currency`（擴充）、`group_buy_status`、`group_leader_application_status`、`notification_type`、`order_status`、`payment_method`、`user_role`，與規格 + 需求追蹤矩陣衝突解法 #2 一致。（`pg_type` 查詢還列出 Supabase 內建 `auth`/`storage` schema 的其他 Enum，如 `aal_level`、`buckettype` 等，屬於 Supabase 平台本身，非本專案建立，不列入計算。）
- **Public schema 內 Constraint 總數：97**
- **Public schema 內 Index 總數：69**

**結論：Migration 內容與 Database Design v2.1（含 2 個已確認的擴充欄位）完全一致，Stage 1 資料庫基礎已在真實 Supabase 環境驗證通過。**

---

## 4. 已知風險／後續留意事項

1. **Session pooler 連線行為**：目前使用 port 5432 的 Session pooler。若後續 Stage 2+ 開發中發現 SQLAlchemy 連線池與 Supavisor pooler 有相容性問題（例如 prepared statement 限制），可能需改用 Direct connection（需先解決 IPv6）或調整 `psycopg` 連線參數。
2. **`.env` 內的資料庫密碼**：已寫入 `backend/.env`（已列在 `.gitignore`，不會被提交），但密碼曾在對話中以明文傳遞，建議專案結束或部署前於 Supabase 後台重設密碼。
3. ~~尚未針對 Migration 的 `downgrade()` 做實際回滾測試。~~
   **已於 2026-08-01 實測，結論：專案改採 forward-only。** `downgrade 0003 → 0002`
   在資料庫存在多角色訂單時**必然失敗**（要重建的 `uq_order_item_order_product`
   唯一約束與 0003 之後的資料結構相斥）。因此正式環境不執行 `downgrade`，
   回退一律以還原備份處理，詳見《部署指南》「Migration 政策」。

---

## 5. 結論

第 1 階段（資料庫 Model、Migration、Enum、Constraint、專案骨架與開發環境）**已在真實環境完整驗證**：
- 開發環境（Python 3.12.10、Node.js v24.18.0）安裝完成。
- `pytest` 7/7 測試通過。
- `alembic upgrade head` 對 Supabase PostgreSQL 執行成功，19 張表／12 個 Enum／97 個 Constraint／69 個 Index 皆已確認存在且與規格一致。

第 1 階段正式完成，可以開始第 2 階段（登入、會員和公開 API）。

---

## 6. 第 2 階段測試結果（2026-07-22）

**對應階段：** 第 2 階段（登入、會員和公開 API）

### 6.1 測試資料庫決策

07_Testing_and_Acceptance_Plan_v1.0 §5 要求「pytest + PostgreSQL test database（專用測試資料庫）」，
但本機沒有安裝 PostgreSQL 或 Docker。已詢問使用者是否要（a）本機安裝 PostgreSQL、
（b）另開 Supabase 測試專案、（c）先用 SQLite 過渡，使用者決議：**直接使用現有
Supabase 專案，直到專案完成為止。**

為避免測試資料寫入正式資料庫，`backend/tests/conftest.py` 改寫為：每個測試在一個
DB Connection 的 Transaction 內執行（SQLAlchemy 2.0 `Session(bind=connection,
join_transaction_mode="create_savepoint")`），測試中呼叫的所有 `db.commit()` 只影響
SAVEPOINT，測試結束後整個 Transaction rollback。

**實測驗證：** 執行完整測試套件（59 個測試，皆會寫入資料）後，直接查詢 Supabase
各資料表（`app_user`、`group_leader_application`、`group_leader_profile`、
`activity`、`product`、`character`、`group_buy`、`group_buy_product`、
`group_order`、`order_item`）筆數皆為 **0**，確認測試不會在正式資料庫留下任何資料。

### 6.2 相依套件修正

`passlib[bcrypt]==1.7.4` 與最新版 `bcrypt`（pip 預設安裝到 5.0.0）不相容，首次執行
註冊 API 測試時因 passlib 內部相容性自我檢測（`detect_wrap_bug`）拋出
`ValueError: password cannot be longer than 72 bytes` 而全數失敗。修正方式：
於 `requirements.txt` 明確釘選 `bcrypt==4.0.1`，重新安裝後密碼雜湊恢復正常。

### 6.3 pytest 實際執行結果

```
59 passed, 20 warnings in 123.71s
```

涵蓋範圍：
- 認證 API（註冊、登入、Email 大小寫不敏感唯一性、密碼確認、聯絡方式必填、
  Token 缺漏／無效／不洩漏帳號存在性）
- 會員 API（個人資料查詢／更新、Email 不可修改、聯絡方式至少保留一項）
- 團主申請 API（申請、重複申請衝突、can_reapply 計算）
- 公開內容 API（活動狀態篩選、活動商品僅顯示上架、商品詳情含角色／圖片／收藏狀態、
  開團可用數量與 effective_status 計算含佔用數量／截止／結單情境、團主公開頁需
  完成資料才可查詢、全站搜尋含空白查詢錯誤處理、搜尋商品僅回傳上架商品）
- 圖片上傳 API（權限依 Category 限制、無效 Category、不支援格式、損壞檔案偵測、
  轉存 WebP）

警告僅為第三方套件 `python-jose` 內部使用已棄用的 `datetime.utcnow()`，非本專案程式碼問題。

### 6.4 結論

第 2 階段（登入、會員和公開 API）已完整實作並於真實 Supabase 環境驗證通過，
可以開始第 3 階段（訂單與交易邏輯）。

---

## 7. 第 3 階段測試結果（2026-07-22）

**對應階段：** 第 3 階段（訂單與交易邏輯：收藏、跟團清單、會員訂單、取消申請、
團主訂單狀態操作、團主取消申請處理、通知）

### 7.1 範圍補充

需求追蹤矩陣 §4 未將「收藏」與「會員通知 API」明確分配到任何階段（矩陣本身遺漏，
非文件衝突）。因收藏與跟團清單同屬會員個人清單管理、且訂單狀態變更會建立系統
通知（需要對應的讀取 API 才能驗證），故一併納入第 3 階段，詳見
`docs/目前進度.txt` 第 3 階段記錄。

### 7.2 pytest 實際執行結果（全專案回歸）

```
100 passed, 141 warnings in 516.28s (0:08:36)
```

本次為全專案回歸測試，涵蓋第 1、2、3 階段全部測試（第 1 階段 7 個 + 第 2 階段
52 個 + 第 3 階段新增 41 個）。警告同樣僅為第三方套件 `python-jose` 的
`datetime.utcnow()` 棄用提示。

第 3 階段新增涵蓋範圍：
- 收藏 API（新增／重複新增 Idempotent、移除／重複移除 Idempotent、下架商品仍列出）
- 跟團清單 API（建立、同商品加量、跨開團衝突與確認替換、數量不足檢查、
  移除最後一項連帶刪除清單、佔用數量影響 is_submittable）
- 會員訂單 API（交易鎖定與歷史快照正確性、團規未勾選拒絕、空清單拒絕、
  下單當下數量被搶占的重新驗證與清單保留、訂單列表/詳情、非本人訂單 404）
- 取消申請 API（僅允許可取消狀態、同時最多一筆待處理、空白原因正規化為 null）
- 團主訂單 API（狀態機各步驟轉換、非法跳轉衝突、拒絕原因必填、
  非本人開團訂單回傳 404 且代碼為 ORDER_NOT_OWNED_BY_GROUP_LEADER、
  團主公開資料未完成時 403）
- 團主取消申請處理（核准後訂單變 cancelled、拒絕後訂單狀態不變且可再次申請、
  重複處理衝突）
- 通知 API（訂單事件建立通知、來源與導向連結計算、未讀數、標記已讀不覆蓋
  原時間、標記他人通知回傳 404）

### 7.3 測試資料庫殘留檢查

全部測試執行完成後，再次直接查詢 Supabase 相關資料表（含
`group_order`、`order_item`、`cancellation_request`、`notification`、
`follow_list`、`follow_list_item`、`product_favorite` 等第 3 階段新增使用的
資料表），筆數皆為 **0**，確認交易 rollback 隔離機制在更複雜的交易鎖定情境下
依然正確運作。

### 7.4 結論

第 3 階段（訂單與交易邏輯）已完整實作並於真實 Supabase 環境驗證通過，
可以開始第 4 階段（團主後台 API）。

---

## 8. 第 4 階段測試結果（2026-07-22）

**對應階段：** 第 4 階段（團主後台 API：團主資料設定、Dashboard、開團 CRUD、
團主公告）

### 8.1 需求追蹤矩陣修正

發現矩陣 §4 開發階段對應表把「團主申請」（模組 3.2）列在 Stage 4，但該功能
實際已在 Stage 2 完成並測試（矩陣內部不一致，非文件間衝突）。已修正矩陣，
詳見 `docs/00_Requirements_Traceability_Matrix.md` 與 `docs/目前進度.txt`
第 4 階段記錄。

### 8.2 pytest 實際執行結果（全專案回歸）

```
137 passed, 200 warnings in 745.95s (0:12:25)
```

本次為全專案回歸測試，涵蓋第 1-4 階段全部測試（第 1-3 階段 100 個 +
第 4 階段新增 37 個）。警告同樣僅為第三方套件 `python-jose` 的
`datetime.utcnow()` 棄用提示。

第 4 階段新增涵蓋範圍：
- 團主資料 API（名稱設定後不可修改、名稱重複衝突、聯絡方式至少保留一項、
  未擁有團主資料時 404）
- 團主 Dashboard API（四張統計卡片數字正確、未完成公開資料時 403）
- 團主開團建立 API（活動未開放、商品下架、商品活動不符、滿贈不支援、
  截止時間早於現在、其他付款方式未填說明、重複商品，皆正確擋下）
- 團主開團查詢/修改 API（未擁有開團 404、無訂單可全改、有訂單後欄位凍結、
  凍結後仍可改截止時間與聯絡方式）
- 開團商品增刪改 API（有訂單時禁止新增/移除、售價凍結、接單上限不可低於
  已占用數量、移除最後一項商品擋下）
- 開團結單 API（結單後不可重複結單）
- 團主公告 API（整體公告/特定開團公告收件人計算、零收件人非公開擋下、
  公開時允許零收件人、修改同步通知內容、關閉公開但零收件人擋下、刪除公告
  連帶移除通知、非本人公告 404）

### 8.3 測試資料庫殘留檢查

全部測試執行完成後，再次直接查詢 Supabase 相關資料表（含
`group_buy`、`group_buy_product`、`announcement` 等第 4 階段新增使用的
資料表），筆數皆為 **0**。

### 8.4 結論

第 4 階段（團主後台 API）已完整實作並於真實 Supabase 環境驗證通過，
可以開始第 5 階段（管理員 API）。

---

## 9. 第 5 階段測試結果（2026-07-22）

**對應階段：** 第 5 階段（管理員 API：活動/商品/角色管理、團主申請審核、
平台公告、管理員 Dashboard、會員/團主唯讀擴充）

### 9.1 pytest 實際執行結果（全專案回歸）

```
174 passed, 268 warnings in 867.35s (0:14:27)
```

本次為全專案回歸測試，涵蓋第 1-5 階段全部測試（第 1-4 階段 137 個 +
第 5 階段新增 37 個）。警告同樣僅為第三方套件 `python-jose` 的
`datetime.utcnow()` 棄用提示。

第 5 階段新增涵蓋範圍：
- 管理員身份驗證（非管理員存取 /admin/* 一律 403 PERMISSION_DENIED）
- 管理員活動 API（建立、依狀態/關鍵字篩選、修改、結束/重新開啟、
  重複結束或重複重新開啟擋下、詳情含商品/開團計數）
- 管理員商品 API（建立時同時指定既有角色與新角色、新角色大小寫不敏感
  自動去重、同活動內重複商品名稱擋下、上下架與重複操作擋下、額外圖片
  新增/修改/刪除/重新排序、重新排序清單不完整時擋下）
- 管理員角色維護 API（建立、重複名稱擋下、查詢建議含關聯商品數、
  修改名稱、無商品關聯可刪除、有關聯擋下刪除）
- 管理員團主申請審核 API（列表與詳情、核准後建立不完整團主資料並通知
  申請人、重複審核擋下、拒絕後會員可再次申請）
- 平台公告 API（建立後通知所有已註冊會員、查詢/修改/刪除、修改同步
  通知內容、刪除公告後通知一併移除）
- 管理員 Dashboard API（四張統計卡片數字正確、目前開團唯讀清單）
- 會員/團主唯讀管理擴充 API（列表與詳情正確回傳團主資料完成度、
  找不到會員時回傳 USER_NOT_FOUND）

### 9.2 測試資料庫殘留檢查

全部測試執行完成後，再次直接查詢 Supabase 全部相關資料表（含
`product_image`、`product_character` 等第 5 階段新增使用的資料表），
筆數皆為 **0**。

### 9.3 結論

第 5 階段（管理員 API）已完整實作並於真實 Supabase 環境驗證通過。

**至此，第 2～5 階段（登入會員/公開 API、訂單交易邏輯、團主後台 API、
管理員 API）後端 API 已全數完成，累計 174 個測試涵蓋 Visitor、Member、
Group Leader、Admin 四種角色的主要業務流程，可以開始第 6 階段
（React 共用版型與公開頁）。**
